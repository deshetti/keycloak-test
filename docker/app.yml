version: '2'
services:
    postgres:
        extends:
            file: postgresql.yml
            service: postgres
    keycloak:
        extends:
            file: keycloak.yml
            service: keycloak

